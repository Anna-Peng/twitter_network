from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project takes user inputted keywords to map out experts within the specified fields',
    author='Ella Franks and Anna Peng',
    license='',
)
